---
phase: 01-authentication-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/auth.js
  - src/components/auth/RecoveryForm.jsx
  - src/components/auth/ResetPasswordForm.jsx
  - src/pages/RecoverPasswordPage.jsx
  - src/pages/ResetPasswordPage.jsx
autonomous: true
requirements: [AUTH-02]
user_setup: []
must_haves:
  truths:
    - "User can recover password through a dedicated recovery flow"
    - "User receives simulated email with recovery link (MVP simulation)"
    - "Token has validity of 15-30 minutes"
    - "User can reset password with valid token"
    - "Invalid or expired token shows error"
  artifacts:
    - path: "src/utils/auth.js"
      provides: "Password recovery functions (generateResetToken, validateResetToken, resetPassword)"
      contains: "generateResetToken|validateResetToken|resetPassword"
    - path: "src/components/auth/RecoveryForm.jsx"
      provides: "Email input form for password recovery"
      contains: "input type=email|Enviar link de recuperação"
    - path: "src/components/auth/ResetPasswordForm.jsx"
      provides: "New password form with token validation"
      contains: "input type=password|Resetar senha"
    - path: "src/pages/RecoverPasswordPage.jsx"
      provides: "Recovery page with glassmorphism styling"
      contains: "backdrop-blur-md bg-white/20"
    - path: "src/pages/ResetPasswordPage.jsx"
      provides: "Reset password page with token validation"
      contains: "backdrop-blur-md bg-white/20|token"
  key_links:
    - from: "src/components/auth/RecoveryForm.jsx"
      to: "src/utils/auth.js"
      via: "generateResetToken() call"
      pattern: "import.*auth.*generateResetToken"
    - from: "src/components/auth/ResetPasswordForm.jsx"
      to: "src/utils/auth.js"
      via: "validateResetToken() and resetPassword() calls"
      pattern: "import.*auth.*validateResetToken|resetPassword"
    - from: "src/pages/ResetPasswordPage.jsx"
      to: "URL token parameter"
      via: "useSearchParams() to read token"
      pattern: "useSearchParams|token"
---

<objective>
Implement password recovery flow (AUTH-02) with simulated email sending (MVP) and token-based password reset functionality.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\siltec-ig\.planning\ROADMAP.md
@D:\siltec-ig\.planning\REQUIREMENTS.md
@D:\siltec-ig\.planning\phases\01-authentication-system\01-CONTEXT.md
@D:\siltec-ig\.planning\phases\01-authentication-system\01-RESEARCH.md
@D:\siltec-ig\.planning\phases\01-authentication-system\01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Password Recovery Functions to Auth Utils</name>
  <files>src/utils/auth.js</files>
  <read_first>
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-CONTEXT.md (decisions D-06, D-07, D-08, D-09)
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-RESEARCH.md (section 5, Password Recovery Flow)
    - D:\siltec-ig\src\utils\auth.js (existing file from Plan 01)
  </read_first>
  <action>
    **Append to existing src/utils/auth.js (from Plan 01) - add these functions:**
    
    ```javascript
    // Password Recovery Functions (MVP - Simulated)
    
    // Generate reset token and store in localStorage
    export function generateResetToken(email) {
      const users = JSON.parse(localStorage.getItem('mockUsers') || '[]');
      const user = users.find(u => u.email === email);
      
      if (!user) return { success: false, error: 'E-mail não encontrado. Verifique o endereço.' };
      
      const token = Math.random().toString(36).substring(2, 15) + Math.random().toString(36).substring(2, 15);
      const expires = Date.now() + 15 * 60 * 1000; // 15 minutes (per D-09)
      
      const resetTokens = JSON.parse(localStorage.getItem('resetTokens') || '{}');
      resetTokens[token] = { email, expires };
      localStorage.setItem('resetTokens', JSON.stringify(resetTokens));
      
      return { success: true, token, email: maskEmail(email) };
    }

    // Validate reset token
    export function validateResetToken(token) {
      const resetTokens = JSON.parse(localStorage.getItem('resetTokens') || '{}');
      const resetData = resetTokens[token];
      
      if (!resetData) return { valid: false, error: 'Token inválido ou já utilizado.' };
      
      if (resetData.expires < Date.now()) {
        // Clean up expired token
        delete resetTokens[token];
        localStorage.setItem('resetTokens', JSON.stringify(resetTokens));
        return { valid: false, error: 'Token expirado. Solicite um novo link de recuperação.' };
      }
      
      return { valid: true, email: resetData.email };
    }

    // Reset password with valid token
    export function resetPassword(token, newPassword) {
      const validation = validateResetToken(token);
      if (!validation.valid) return validation;
      
      const resetTokens = JSON.parse(localStorage.getItem('resetTokens') || '{}');
      const resetData = resetTokens[token];
      
      // Update user password
      const users = JSON.parse(localStorage.getItem('mockUsers') || '[]');
      const userIndex = users.findIndex(u => u.email === resetData.email);
      
      if (userIndex === -1) return { success: false, error: 'Usuário não encontrado.' };
      
      users[userIndex].password = newPassword;
      localStorage.setItem('mockUsers', JSON.stringify(users));
      
      // Invalidate token after use (one-time use)
      delete resetTokens[token];
      localStorage.setItem('resetTokens', JSON.stringify(resetTokens));
      
      return { success: true };
    }

    // Helper: Mask email for display (e.g., a***@e***.com)
    function maskEmail(email) {
      const [local, domain] = email.split('@');
      const maskedLocal = local.charAt(0) + '***';
      const domainParts = domain.split('.');
      const maskedDomain = domainParts[0].charAt(0) + '***.' + domainParts.slice(1).join('.');
      return `${maskedLocal}@${maskedDomain}`;
    }
    ```
    
    **Important:** This appends to the existing auth.js file created in Plan 01. The file should now export:
    - initMockUsers, createMockJWT, login, logout, getToken, decodeToken, isTokenExpired (from Plan 01)
    - generateResetToken, validateResetToken, resetPassword (added in this task)
  </action>
  <verify>
    <automated>
      cd D:\siltec-ig; if (Test-Path "src/utils/auth.js") { 
        Get-Content "src/utils/auth.js" | Select-String "export function generateResetToken|export function validateResetToken|export function resetPassword" 
      } else { 
        echo "auth.js not found - run Plan 01 first" 
      }
    </automated>
  </verify>
  <acceptance_criteria>
    - src/utils/auth.js contains export function generateResetToken(email)
    - generateResetToken returns { success: true, token, email: maskedEmail } for valid email
    - generateResetToken returns { success: false, error: '...' } for invalid email
    - Token stored in localStorage under 'resetTokens' key with 15-minute expiry
    - Token format: random string (e.g., 'abc123xyz789')
    - Email is masked in return value (e.g., 'a***@e***.com')
    - src/utils/auth.js contains export function validateResetToken(token)
    - validateResetToken returns { valid: true, email } for valid, non-expired token
    - validateResetToken returns { valid: false, error: '...' } for invalid or expired token
    - Expired tokens are removed from localStorage after validation attempt
    - src/utils/auth.js contains export function resetPassword(token, newPassword)
    - resetPassword updates user password in mockUsers array
    - resetPassword invalidates token after successful password change (one-time use)
    - resetPassword returns { success: true } on success
    - Masked email format shows first character + '***' @ first domain char + '***.com'
  </acceptance_criteria>
  <done>Password recovery functions added to auth.js with token generation, validation, and password reset capabilities. Tokens expire in 15 minutes per D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Create Recovery and Reset Password Pages</name>
  <files>src/components/auth/RecoveryForm.jsx, src/components/auth/ResetPasswordForm.jsx, src/pages/RecoverPasswordPage.jsx, src/pages/ResetPasswordPage.jsx</files>
  <read_first>
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-CONTEXT.md (decisions D-06, D-07, D-08)
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-UI-SPEC.md (Password Recovery Page section, Copywriting contract)
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-RESEARCH.md (section 5, Recovery flow)
  </read_first>
  <action>
    **Step1: Create src/components/auth/RecoveryForm.jsx:**
    
    ```jsx
    import React, { useState } from 'react';
    import { Link } from 'react-router-dom';
    import { generateResetToken } from '../../utils/auth';

    export default function RecoveryForm() {
      const [email, setEmail] = useState('');
      const [error, setError] = useState('');
      const [success, setSuccess] = useState(null);
      const [loading, setLoading] = useState(false);

      const handleSubmit = (e) => {
        e.preventDefault();
        setError('');
        setSuccess(null);
        
        if (!email) {
          setError('Por favor, informe seu e-mail.');
          return;
        }

        setLoading(true);
        
        // Simulate API delay
        setTimeout(() => {
          const result = generateResetToken(email);
          
          if (result.success) {
            // MVP: Show token on screen (per D-08, simulation)
            setSuccess({
              message: `E-mail enviado para ${result.email}`,
              debugToken: result.token // Only for MVP testing
            });
          } else {
            setError(result.error);
          }
          
          setLoading(false);
        }, 500);
      };

      return (
        <form onSubmit={handleSubmit} className="space-y-4">
          {!success && (
            <>
              <div>
                <label htmlFor="email" className="block text-sm font-medium mb-1">
                  E-mail
                </label>
                <input
                  id="email"
                  type="email"
                  value={email}
                  onChange={(e) => setEmail(e.target.value)}
                  className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg focus:border-blue-400 focus:outline-none"
                  placeholder="seu@email.com"
                  required
                />
              </div>

              {error && (
                <div className="text-red-500 text-sm text-center py-2">
                  {error}
                </div>
              )}

              <button
                type="submit"
                disabled={loading}
                className="w-full bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg transition-colors disabled:opacity-50"
              >
                {loading ? 'Enviando...' : 'Enviar link de recuperação'}
              </button>
            </>
          )}

          {success && (
            <div className="text-center space-y-4">
              <div className="text-green-600 text-sm font-medium py-2">
                {success.message}
              </div>
              
              {/* MVP Simulation: Show token for testing (per D-08) */}
              <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-4 text-left">
                <p className="text-xs text-yellow-800 font-medium mb-2">
                  [MVP Simulation] Use this token for testing:
                </p>
                <code className="text-xs break-all text-yellow-900 bg-yellow-100 px-2 py-1 rounded">
                  {success.debugToken}
                </code>
                <p className="text-xs text-yellow-700 mt-2">
                  Reset URL: {'{base}'}/reset-password?token={success.debugToken}
                </p>
              </div>
            </div>
          )}

          <div className="text-center">
            <Link
              to="/login"
              className="text-sm text-blue-500 hover:underline"
            >
              Voltar ao login
            </Link>
          </div>
        </form>
      );
    }
    ```

    **Step2: Create src/components/auth/ResetPasswordForm.jsx:**
    
    ```jsx
    import React, { useState } from 'react';
    import { useSearchParams, useNavigate } from 'react-router-dom';
    import { validateResetToken, resetPassword } from '../../utils/auth';

    export default function ResetPasswordForm() {
      const [searchParams] = useSearchParams();
      const navigate = useNavigate();
      const token = searchParams.get('token');
      
      const [password, setPassword] = useState('');
      const [confirmPassword, setConfirmPassword] = useState('');
      const [error, setError] = useState('');
      const [success, setSuccess] = useState(false);
      const [loading, setLoading] = useState(false);
      const [tokenValid, setTokenValid] = useState(null);

      // Validate token on mount
      React.useEffect(() => {
        if (!token) {
          setTokenValid(false);
          setError('Token não fornecido.');
          return;
        }
        
        const validation = validateResetToken(token);
        setTokenValid(validation.valid);
        if (!validation.valid) {
          setError(validation.error);
        }
      }, [token]);

      const handleSubmit = (e) => {
        e.preventDefault();
        setError('');
        
        if (!password || !confirmPassword) {
          setError('Por favor, preencha todos os campos.');
          return;
        }
        
        if (password.length < 6) {
          setError('A senha deve ter pelo menos 6 caracteres.');
          return;
        }
        
        if (password !== confirmPassword) {
          setError('As senhas não coincidem.');
          return;
        }

        setLoading(true);
        
        setTimeout(() => {
          const result = resetPassword(token, password);
          
          if (result.success) {
            setSuccess(true);
            setTimeout(() => {
              navigate('/login');
            }, 2000);
          } else {
            setError(result.error || 'Erro ao resetar senha.');
          }
          
          setLoading(false);
        }, 500);
      };

      if (tokenValid === false) {
        return (
          <div className="text-center space-y-4">
            <div className="text-red-500 text-sm font-medium py-2">
              {error || 'Token inválido ou expirado.'}
            </div>
            <a
              href="/recover-password"
              className="text-sm text-blue-500 hover:underline"
            >
              Solicitar novo link de recuperação
            </a>
          </div>
        );
      }

      if (success) {
        return (
          <div className="text-center space-y-4">
            <div className="text-green-600 text-sm font-medium py-2">
              Senha alterada com sucesso! Redirecionando para login...
            </div>
          </div>
        );
      }

      return (
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label htmlFor="password" className="block text-sm font-medium mb-1">
              Nova Senha
            </label>
            <input
              id="password"
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg focus:border-blue-400 focus:outline-none"
              placeholder="••••••••"
              required
              minLength={6}
            />
          </div>

          <div>
            <label htmlFor="confirmPassword" className="block text-sm font-medium mb-1">
              Confirmar Nova Senha
            </label>
            <input
              id="confirmPassword"
              type="password"
              value={confirmPassword}
              onChange={(e) => setConfirmPassword(e.target.value)}
              className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg focus:border-blue-400 focus:outline-none"
              placeholder="••••••••"
              required
              minLength={6}
            />
          </div>

          {error && (
            <div className="text-red-500 text-sm text-center py-2">
              {error}
            </div>
          )}

          <button
            type="submit"
            disabled={loading}
            className="w-full bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg transition-colors disabled:opacity-50"
          >
            {loading ? 'Resetando...' : 'Resetar senha'}
          </button>
        </form>
      );
    }
    ```

    **Step3: Create src/pages/RecoverPasswordPage.jsx:**
    
    ```jsx
    import React from 'react';
    import { Link } from 'react-router-dom';
    import RecoveryForm from '../components/auth/RecoveryForm';

    export default function RecoverPasswordPage() {
      return (
        <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4">
          <div className="w-full max-w-md backdrop-blur-md bg-white/20 border border-white/30 shadow-glass rounded-xl p-8">
            <div className="text-center mb-8">
              <span className="material-symbols-outlined text-4xl text-gray-600 mb-4">
                lock_reset
              </span>
              <h1 className="text-2xl font-bold text-gray-800">Recuperar Senha</h1>
              <p className="text-gray-600 mt-2">
                Informe seu e-mail para receber o link de recuperação
              </p>
            </div>
            
            <RecoveryForm />
          </div>
        </div>
      );
    }
    ```

    **Step4: Create src/pages/ResetPasswordPage.jsx:**
    
    ```jsx
    import React from 'react';
    import { Link } from 'react-router-dom';
    import ResetPasswordForm from '../components/auth/ResetPasswordForm';

    export default function ResetPasswordPage() {
      return (
        <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4">
          <div className="w-full max-w-md backdrop-blur-md bg-white/20 border border-white/30 shadow-glass rounded-xl p-8">
            <div className="text-center mb-8">
              <span className="material-symbols-outlined text-4xl text-gray-600 mb-4">
                key
              </span>
              <h1 className="text-2xl font-bold text-gray-800">Nova Senha</h1>
              <p className="text-gray-600 mt-2">
                Defina sua nova senha de acesso
              </p>
            </div>
            
            <ResetPasswordForm />
            
            <div className="mt-6 text-center">
              <Link
                to="/login"
                className="text-sm text-blue-500 hover:underline"
              >
                Voltar ao login
              </Link>
            </div>
          </div>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>
      cd D:\siltec-ig; 
      if (Test-Path "src/components/auth/RecoveryForm.jsx") { Get-Content "src/components/auth/RecoveryForm.jsx" | Select-String "type=\"email\"|Enviar link de recuperação|E-mail enviado para" } else { echo "RecoveryForm.jsx not found" }
      if (Test-Path "src/components/auth/ResetPasswordForm.jsx") { Get-Content "src/components/auth/ResetPasswordForm.jsx" | Select-String "type=\"password\"|Resetar senha|useSearchParams" } else { echo "ResetPasswordForm.jsx not found" }
      if (Test-Path "src/pages/RecoverPasswordPage.jsx") { Get-Content "src/pages/RecoverPasswordPage.jsx" | Select-String "backdrop-blur-md bg-white/20|RecoveryForm" } else { echo "RecoverPasswordPage.jsx not found" }
      if (Test-Path "src/pages/ResetPasswordPage.jsx") { Get-Content "src/pages/ResetPasswordPage.jsx" | Select-String "backdrop-blur-md bg-white/20|ResetPasswordForm" } else { echo "ResetPasswordPage.jsx not found" }
    </automated>
  </verify>
  <acceptance_criteria>
    - src/components/auth/RecoveryForm.jsx exists with: input type="email", "Enviar link de recuperação" button
    - RecoveryForm calls generateResetToken(email) on submit
    - RecoveryForm shows success message "E-mail enviado para ***@***.com" on success
    - RecoveryForm shows token on screen for MVP testing (per D-08)
    - RecoveryForm shows error "E-mail não encontrado. Verifique o endereço." for invalid email
    - RecoveryForm has "Voltar ao login" link to /login
    - src/components/auth/ResetPasswordForm.jsx exists with: input type="password" (x2 for new password and confirm)
    - ResetPasswordForm reads token from URL parameter using useSearchParams()
    - ResetPasswordForm validates token on mount using validateResetToken()
    - ResetPasswordForm shows error for invalid/expired token with link to /recover-password
    - ResetPasswordForm calls resetPassword(token, password) on submit
    - ResetPasswordForm validates password length >= 6 characters
    - ResetPasswordForm validates passwords match before submission
    - Success shows "Senha alterada com sucesso!" and redirects to /login after 2 seconds
    - src/pages/RecoverPasswordPage.jsx exists with glassmorphism styling (backdrop-blur-md, bg-white/20)
    - RecoverPasswordPage renders RecoveryForm component
    - RecoverPasswordPage has lock_reset icon (Material Symbols)
    - src/pages/ResetPasswordPage.jsx exists with glassmorphism styling
    - ResetPasswordPage renders ResetPasswordForm component
    - ResetPasswordPage has key icon (Material Symbols)
    - Both pages have "Voltar ao login" link
  </acceptance_criteria>
  <done>Password recovery flow implemented with RecoveryForm, ResetPasswordForm, and pages with glassmorphism styling. MVP simulation shows token on screen per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RecoveryForm → generateResetToken | Email validation against localStorage |
| ResetPasswordForm → validateResetToken | Token validation from URL parameter |
| ResetPasswordForm → resetPassword | Password update in localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | RecoveryForm.jsx | Accept | MVP shows token on screen for testing only (per D-08) |
| T-01-09 | Spoofing | Reset Token | Accept | 15-minute expiry (D-09); MVP simulation only |
| T-01-10 | Tampering | Password Reset | Accept | Client-side only MVP; Supabase will handle server-side validation |
</threat_model>

<verification>
- `npm test` passes (if tests exist)
- Access /recover-password → shows email input form
- Submit valid email (admin@siltec.com) → shows "E-mail enviado para a***@s***.com" with token
- Submit invalid email → shows error "E-mail não encontrado"
- Click reset link with token → /reset-password?token=xyz
- Valid token → shows password reset form
- Invalid/expired token → shows error with link to /recover-password
- Submit new password → "Senha alterada com sucesso!" and redirect to /login
- Use old password → fails; use new password → succeeds
- Token can only be used once (one-time use)
</verification>

<success_criteria>
- [ ] User can request password recovery with email (AUTH-02)
- [ ] System validates email exists in mockUsers
- [ ] Simulated email shows message "E-mail enviado para ***@***.com" (AUTH-02)
- [ ] Token displayed on screen for MVP testing (per D-08)
- [ ] Reset link includes token as URL parameter (per D-07)
- [ ] Token expires after 15 minutes (per D-09)
- [ ] User can set new password with valid token
- [ ] Invalid/expired token shows error message
- [ ] Token is one-time use (invalidated after password reset)
- [ ] Recovery and Reset pages have glassmorphism styling
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-system/01-03-SUMMARY.md`
</output>
