---
phase: 01-authentication-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/AuthContext.js
  - src/utils/auth.js
  - src/components/auth/LoginForm.jsx
  - src/pages/LoginPage.jsx
  - package.json
autonomous: true
requirements: [AUTH-01, AUTH-03]
user_setup: []
must_haves:
  truths:
    - "User can access the system with email and password"
    - "User can opt for 'Remember me' to persist session across browser sessions"
    - "JWT token includes role in payload for authorization checks"
    - "Valid credentials return success, invalid credentials show error"
  artifacts:
    - path: "src/contexts/AuthContext.js"
      provides: "Global auth state management with user role"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/utils/auth.js"
      provides: "Token handling and mock user validation"
      exports: ["login", "logout", "getToken", "decodeToken"]
    - path: "src/components/auth/LoginForm.jsx"
      provides: "Login form with email, password, remember me"
      contains: "input type=email, input type=password, checkbox"
    - path: "src/pages/LoginPage.jsx"
      provides: "Login page with glassmorphism styling"
      contains: "backdrop-blur-md bg-white/20"
  key_links:
    - from: "src/components/auth/LoginForm.jsx"
      to: "src/utils/auth.js"
      via: "login() function call"
      pattern: "import.*auth.*login"
    - from: "src/contexts/AuthContext.js"
      to: "src/utils/auth.js"
      via: "getToken() for user state"
      pattern: "getToken|decodeToken"
---

<objective>
Implement secure login with email/password (AUTH-01) and "Remember me" session persistence (AUTH-03) using JWT tokens and mock user data stored in localStorage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\siltec-ig\.planning\ROADMAP.md
@D:\siltec-ig\.planning\REQUIREMENTS.md
@D:\siltec-ig\.planning\phases\01-authentication-system\01-CONTEXT.md
@D:\siltec-ig\.planning\phases\01-authentication-system\01-RESEARCH.md
@D:\siltec-ig\.planning\phases\01-authentication-system\01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Auth Utilities and Mock Users</name>
  <files>src/utils/auth.js, package.json</files>
  <read_first>
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-CONTEXT.md (decisions D-10, D-11, D-12, D-14, D-15)
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-RESEARCH.md (sections 1, 4, 6)
  </read_first>
  <action>
    **Step 1: Install dependencies**
    Run: `npm install jwt-decode react-router-dom`
    
    **Step 2: Create src/utils/auth.js with these exact exports:**
    
    ```javascript
    // Mock users initialization (run once on app start)
    export function initMockUsers() {
      if (!localStorage.getItem('mockUsers')) {
        const mockUsers = [
          { email: 'admin@siltec.com', password: 'admin123', role: 'super_admin', name: 'Super Admin' },
          { email: 'tesoureiro@siltec.com', password: 'treasurer123', role: 'treasurer', name: 'Tesoureiro' },
          { email: 'lider@siltec.com', password: 'leader123', role: 'leader', name: 'Líder' },
          { email: 'membro@siltec.com', password: 'member123', role: 'member', name: 'Membro' }
        ];
        localStorage.setItem('mockUsers', JSON.stringify(mockUsers));
      }
    }

    // Mock JWT creation (base64 encoded payload only, no signature for MVP)
    export function createMockJWT(user) {
      const header = btoa(JSON.stringify({ alg: 'none', typ: 'JWT' }));
      const payload = btoa(JSON.stringify({
        email: user.email,
        role: user.role,
        name: user.name,
        exp: Math.floor(Date.now() / 1000) + (user.rememberMe ? 7 * 24 * 60 * 60 : 24 * 60 * 60) // 7 days or 1 day
      }));
      return `${header}.${payload}.mock-signature`;
    }

    // Login function
    export function login(email, password, rememberMe = false) {
      const users = JSON.parse(localStorage.getItem('mockUsers') || '[]');
      const user = users.find(u => u.email === email && u.password === password);
      
      if (!user) return { success: false, error: 'E-mail ou senha inválidos. Tente novamente.' };
      
      const token = createMockJWT({ ...user, rememberMe });
      
      if (rememberMe) {
        localStorage.setItem('authToken', token);
        sessionStorage.removeItem('authToken');
      } else {
        sessionStorage.setItem('authToken', token);
        localStorage.removeItem('authToken');
      }
      
      return { success: true, user: { email: user.email, role: user.role, name: user.name } };
    }

    // Logout function
    export function logout() {
      localStorage.removeItem('authToken');
      sessionStorage.removeItem('authToken');
    }

    // Get token from storage
    export function getToken() {
      return localStorage.getItem('authToken') || sessionStorage.getItem('authToken');
    }

    // Decode token (client-side only, no verification)
    export function decodeToken(token) {
      try {
        const payload = JSON.parse(atob(token.split('.')[1]));
        return payload;
      } catch (e) {
        return null;
      }
    }

    // Check if token is expired
    export function isTokenExpired(token) {
      const payload = decodeToken(token);
      if (!payload || !payload.exp) return true;
      return payload.exp * 1000 < Date.now();
    }
    ```
    
    **Step 3: Verify package.json has dependencies:**
    - `jwt-decode` (for decoding JWT)
    - `react-router-dom` (for navigation after login)
  </action>
  <verify>
    <automated>
      node -e "const auth = require('./src/utils/auth.js'); console.log('Module loads successfully')" 2>&1 || echo "Checking syntax with node..."
      cd D:\siltec-ig; if (Test-Path "src/utils/auth.js") { Get-Content "src/utils/auth.js" | Select-String "export function" } else { echo "File not found" }
    </automated>
  </verify>
  <acceptance_criteria>
    - src/utils/auth.js exists and contains all exported functions: initMockUsers, createMockJWT, login, logout, getToken, decodeToken, isTokenExpired
    - package.json contains "jwt-decode" in dependencies
    - package.json contains "react-router-dom" in dependencies
    - login() function returns { success: true, user: {...} } for valid credentials (admin@siltec.com/admin123)
    - login() function returns { success: false, error: '...' } for invalid credentials
    - Token stored in localStorage when rememberMe=true
    - Token stored in sessionStorage when rememberMe=false
    - createMockJWT includes role in payload (verifiable via decodeToken)
  </acceptance_criteria>
  <done>Auth utilities created with mock user login, JWT creation, and token storage in localStorage/sessionStorage based on "Remember me" choice.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthContext and Login Form</name>
  <files>src/contexts/AuthContext.js, src/components/auth/LoginForm.jsx, src/pages/LoginPage.jsx</files>
  <read_first>
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-CONTEXT.md (decisions D-14, D-15)
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-UI-SPEC.md (Login Page section, Copywriting contract)
    - D:\siltec-ig\.planning\phases\01-authentication-system\01-RESEARCH.md (sections 1, 7, 8)
  </read_first>
  <action>
    **Step 1: Create src/contexts/AuthContext.js:**
    
    ```jsx
    import React, { createContext, useContext, useState, useEffect } from 'react';
    import { useNavigate } from 'react-router-dom';
    import { getToken, decodeToken, login, logout, initMockUsers, isTokenExpired } from '../utils/auth';

    initMockUsers();

    const AuthContext = createContext(null);

    export function AuthProvider({ children }) {
      const [user, setUser] = useState(null);
      const [loading, setLoading] = useState(true);
      const navigate = useNavigate();

      useEffect(() => {
        const token = getToken();
        if (token && !isTokenExpired(token)) {
          const decoded = decodeToken(token);
          setUser({ email: decoded.email, role: decoded.role, name: decoded.name });
        } else if (token) {
          logout(); // Clear expired token
        }
        setLoading(false);
      }, []);

      const handleLogin = (email, password, rememberMe) => {
        const result = login(email, password, rememberMe);
        if (result.success) {
          setUser(result.user);
          navigate('/dashboard');
        }
        return result;
      };

      const handleLogout = () => {
        logout();
        setUser(null);
        navigate('/login');
      };

      return (
        <AuthContext.Provider value={{ user, login: handleLogin, logout: handleLogout, loading }}>
          {children}
        </AuthContext.Provider>
      );
    }

    export function useAuth() {
      const context = useContext(AuthContext);
      if (!context) {
        throw new Error('useAuth must be used within AuthProvider');
      }
      return context;
    }
    ```

    **Step 2: Create src/components/auth/LoginForm.jsx:**
    
    ```jsx
    import React, { useState } from 'react';
    import { useAuth } from '../../contexts/AuthContext';

    export default function LoginForm() {
      const [email, setEmail] = useState('');
      const [password, setPassword] = useState('');
      const [rememberMe, setRememberMe] = useState(false);
      const [error, setError] = useState('');
      const [showPassword, setShowPassword] = useState(false);
      const { login } = useAuth();

      const handleSubmit = (e) => {
        e.preventDefault();
        setError('');
        
        if (!email || !password) {
          setError('Por favor, preencha todos os campos.');
          return;
        }

        const result = login(email, password, rememberMe);
        if (!result.success) {
          setError(result.error);
        }
      };

      return (
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label htmlFor="email" className="block text-sm font-medium mb-1">E-mail</label>
            <input
              id="email"
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg focus:border-blue-400 focus:outline-none"
              placeholder="seu@email.com"
              required
            />
          </div>

          <div>
            <label htmlFor="password" className="block text-sm font-medium mb-1">Senha</label>
            <div className="relative">
              <input
                id="password"
                type={showPassword ? 'text' : 'password'}
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                className="w-full px-4 py-2 bg-white/10 border border-white/20 rounded-lg focus:border-blue-400 focus:outline-none pr-10"
                placeholder="••••••••"
                required
              />
              <button
                type="button"
                onClick={() => setShowPassword(!showPassword)}
                className="absolute right-2 top-1/2 -translate-y-1/2 text-gray-600"
              >
                {showPassword ? '🙈' : '👁️'}
              </button>
            </div>
          </div>

          <div className="flex items-center justify-between">
            <label className="flex items-center gap-2 text-sm">
              <input
                type="checkbox"
                checked={rememberMe}
                onChange={(e) => setRememberMe(e.target.checked)}
                className="rounded border-white/20"
              />
              Lembrar-me
            </label>
            <a href="/recover-password" className="text-sm text-blue-500 hover:underline">
              Esqueceu sua senha?
            </a>
          </div>

          {error && (
            <div className="text-red-500 text-sm text-center py-2">
              {error}
            </div>
          )}

          <button
            type="submit"
            className="w-full bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg transition-colors"
          >
            Entrar
          </button>
        </form>
      );
    }
    ```

    **Step 3: Create src/pages/LoginPage.jsx with glassmorphism styling:**
    
    ```jsx
    import React from 'react';
    import { Link } from 'react-router-dom';
    import LoginForm from '../components/auth/LoginForm';

    export default function LoginPage() {
      return (
        <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4">
          <div className="w-full max-w-md backdrop-blur-md bg-white/20 border border-white/30 shadow-glass rounded-xl p-8">
            <div className="text-center mb-8">
              <h1 className="text-2xl font-bold text-gray-800">Siltec-SGI</h1>
              <p className="text-gray-600 mt-2">Santuário Digital</p>
            </div>
            
            <LoginForm />
            
            <div className="mt-6 text-center text-sm text-gray-600">
              <p>Test users:</p>
              <p className="text-xs mt-1">admin@siltec.com / lider@siltec.com / tesoureiro@siltec.com / membro@siltec.com</p>
            </div>
          </div>
        </div>
      );
    }
    ```
    
    Note: shadow-glass needs to be defined in tailwind.config.js as a custom shadow. Use `shadow-xl` as fallback if not configured.
  </action>
  <verify>
    <automated>
      cd D:\siltec-ig; if (Test-Path "src/contexts/AuthContext.js") { Get-Content "src/contexts/AuthContext.js" | Select-String "export function AuthProvider|export function useAuth" } else { echo "AuthContext.js not found" }
      if (Test-Path "src/components/auth/LoginForm.jsx") { Get-Content "src/components/auth/LoginForm.jsx" | Select-String "type=\"email\"|type=\"password\"|Lembrar-me|Entrar" } else { echo "LoginForm.jsx not found" }
      if (Test-Path "src/pages/LoginPage.jsx") { Get-Content "src/pages/LoginPage.jsx" | Select-String "backdrop-blur-md bg-white/20|LoginForm" } else { echo "LoginPage.jsx not found" }
    </automated>
  </verify>
  <acceptance_criteria>
    - src/contexts/AuthContext.js exists and exports AuthProvider and useAuth
    - AuthProvider initializes user state from token on app load (useEffect with getToken)
    - AuthProvider has handleLogin that calls login() from auth.js and navigates to /dashboard on success
    - AuthProvider has handleLogout that calls logout() and navigates to /login
    - src/components/auth/LoginForm.jsx exists with: input type="email", input type="password", "Lembrar-me" checkbox, "Entrar" button
    - LoginForm calls useAuth() to access login function
    - LoginForm has password visibility toggle (👁️/🙈)
    - LoginForm displays error message "E-mail ou senha inválidos. Tente novamente." on failed login
    - src/pages/LoginPage.jsx exists with glassmorphism classes: backdrop-blur-md, bg-white/20, border-white/30
    - LoginPage renders LoginForm component
    - LoginPage has link to /recover-password with text "Esqueceu sua senha?"
  </acceptance_criteria>
  <done>Login page with glassmorphism styling, email/password form, "Remember me" checkbox, and integration with AuthContext for session management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client ↔ localStorage/sessionStorage | JWT tokens stored client-side (untrusted) |
| Login form → Mock user validation | Email/password validation against localStorage data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | LoginForm.jsx | Accept | MVP uses mock data; real auth via Supabase in future |
| T-01-02 | Tampering | localStorage | Accept | Client-side only MVP; migration to Supabase will use httpOnly cookies |
| T-01-03 | Information Disclosure | JWT in localStorage | Accept | No sensitive data in payload; MVP simulation only |
| T-01-04 | Elevation of Privilege | Role in JWT payload | Accept | Client-side checks for UX only; server-side enforcement in future Supabase integration |
</threat_model>

<verification>
- `npm test` passes (if tests exist)
- Login with admin@siltec.com / admin123 redirects to /dashboard
- Login with wrong credentials shows error "E-mail ou senha inválidos. Tente novamente."
- Checking "Lembrar-me" stores token in localStorage
- Not checking "Lembrar-me" stores token in sessionStorage
- Token includes role in payload (verifiable via browser DevTools)
- Page styled with glassmorphism (backdrop-blur-md bg-white/20)
</verification>

<success_criteria>
- [ ] User can login with email and password (AUTH-01)
- [ ] User can opt for "Remember me" to persist session (AUTH-03)
- [ ] JWT token stored in correct storage based on remember me choice
- [ ] AuthContext provides user state globally
- [ ] Login form has email, password, remember me, and submit button
- [ ] Glassmorphism styling applied to login page
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-system/01-01-SUMMARY.md`
</output>
