---
wave: 1
depends_on: []
requirements_addressed: [EVT-CRUD-01, EVT-CRUD-02, EVT-CRUD-03, EVT-CRUD-04, EVT-CRUD-05, EVT-CRUD-06]
files_modified: [src/pages/EventsPage.jsx, src/services/storage.js]
autonomous: true
---

## Plan 08-01: Events CRUD

<objective>

Implementar operações CRUD para eventos com gestão de capacidades, inscrições e eventos recorrentes.

</objective>

<read_first>

- src/pages/EventsPage.jsx (current events page structure)
- src/services/storage.js (existing storage service)
- src/pages/MembersPage.jsx (CRUD pattern reference)
- .planning/REQUIREMENTS.md (v2.0 requirements)

</read_first>

<action>

1. **Expandir storage service:**
   - Adicionar: `getEvents()`, `saveEvent(event)`, `updateEvent(id, data)`, `deleteEvent(id)`
   - Adicionar: `getRegistrations(eventId)`, `addRegistration(eventId, memberId)`, `removeRegistration(eventId, memberId)`
   - Adicionar: `approveRegistration(eventId, memberId)`, `rejectRegistration(eventId, memberId)`
   - Adicionar: `createRecurringEvent(event, weeks)`

2. **Criar EventFormCreate:**
   - Arquivo: `src/components/crud/EventFormCreate.jsx`
   - Campos: título, data, horário, descrição, capacidade, categoria
   - Categorias: Retiros, Feiras, Ações Comunitárias, Eventos

3. **Criar EventFormEdit:**
   - Arquivo: `src/components/crud/EventFormEdit.jsx`
   - Mesmo schema do Create, pré-preenchido

4. **Atualizar EventsPage:**
   - Substituir mock events por storage.getEvents()
   - Adicionar botão "Criar Evento" no header
   - Adicionar Editar/Excluir botões em cada card
   - Adicionar gestão de inscrições (aprovar/rejeitar)
   - Adicionar opção de evento semanal (recorrente)

5. **Integrar modais e toast:**
   - Reutilizar componentes da Phase 7

</action>

<acceptance_criteria>

- [ ] storage.js exports: getEvents, saveEvent, updateEvent, deleteEvent
- [ ] storage.js exports: getRegistrations, addRegistration, approve, reject
- [ ] storage.js exports: createRecurringEvent para eventos semanais
- [ ] EventFormCreate valida: título obrigatório, capacidade > 0
- [ ] EventFormEdit carrega dados do evento
- [ ] EventsPageusa storage em vez de mock data
- [ ] Gestão de inscrições funcional
- [ ] Eventos recorrentes funcionam

</acceptance_criteria>

<verification>

1. Abrir EventsPage - eventos do localStorage
2. Clicar "Criar Evento" - Modal abre
3. Criar evento - aparece na lista
4. Editar evento - dados pré-preenchidos
5. Inscrever membro - lista atualiza
6. Aprovar/rejeitar inscrição - status atualiza
7. Criar evento recorrente -copiado semanalmente

</verification>

<must_haves>

- Sistema persiste eventos em localStorage
- Usuário pode criar/editar/excluir eventos
- Gestão de inscrições funciona
- Eventos recorrentes (semanal)

</must_haves>